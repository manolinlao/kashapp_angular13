export declare enum InterfaceNameEnum {
    /** Common interface. Every device implements this interface. */
    COMMON = "common",
    /** CardReader interface. */
    CARD_READER = "cardReader",
    /** CashAcceptor interface. */
    CASH_ACCEPTOR = "cashAcceptor",
    /** CashDispenser interface. */
    CASH_DISPENSER = "cashDispenser",
    /** CashManagement interface. */
    CASH_MANAGEMENT = "cashManagement",
    /** PinPad interface. */
    PIN_PAD = "pinPad",
    /** Crypto interface. */
    CRYPTO = "crypto",
    /** KeyManagement interface. */
    KEY_MANAGEMENT = "keyManagement",
    /** Keyboard interface. */
    KEYBOARD = "keyboard",
    /** TextTerminal interface. */
    TEXT_TERMINAL = "textTerminal",
    /** Printer interface. */
    PRINTER = "printer",
    /** BarcodeReader interface. */
    BARCODE_READER = "barcodeReader",
    /** Lights interface. */
    LIGHTS = "lights",
    /** Auxiliaries interface. */
    AUXILIARIES = "auxiliaries",
    /** VendorMode interface. */
    VENDOR_MODE = "vendorMode",
    /** VendorApplication interface. */
    VENDOR_APPLICATION = "vendorApplication",
    /** Storage interface */
    STORAGE = "storage"
}
